<?php 

    class Cliente extends Persona{

        // Atributos
        protected $nroCliente;
        protected $direccion;
        protected $telefono;

        // Constructor
        public function __construct($nombre,$apellido,$dni,$nroCliente,$direccion,$telefono){
            parent::__construct($nombre,$apellido,$dni);
            $this->nroCliente = $nroCliente;
            $this->direccion = $direccion;
            $this->telefono = $telefono;
        }

        // Metodos
        
    }

        class Cliente extends Persona{

        // Atributos
        protected $nroCliente;
        protected $direccion;
        protected $telefono;

        // Constructor
        public function __construct($nombre,$apellido,$dni,$nroCliente,$direccion,$telefono){
            parent::__construct($nombre,$apellido,$dni);
            $this->nroCliente = $nroCliente;
            $this->direccion = $direccion;
            $this->telefono = $telefono;
        }

        // Metodos
        
        
    }








?>